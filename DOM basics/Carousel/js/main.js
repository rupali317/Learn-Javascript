const nextButton = document.querySelector('.js-next-button')
const previousButton = document.querySelector('.js-previous-button')

// 1. Updating the left styles of all the carousel__slide
//  a. Get the list of all carousel__slide, loop through them and substract 800 from their current value
// 2. Check of the last instance of carousel__slide is reached, True -> hide the nextButton
nextButton.addEventListener('click', () => {})

previousButton.addEventListener('click', () => {})
