console.log(window.document)
console.log(document)

/*
BOM = Browser Object Model = Javascripts objects provided by the browser
DOM = Document Object Model = the HTML structure represented as tree of objects
The DOM organizes the document as a hierarchical tree, with the document object at the root.
*/
